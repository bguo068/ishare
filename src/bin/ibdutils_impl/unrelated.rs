use super::args::*;

pub fn main_unrelated(args: &Commands) {
    if let Commands::GetUnrelated {
        genome_info,
        sample_lst,
        fmt,
        ibd_dir,
        theshold,
        out,
    } = args
    {}
}
